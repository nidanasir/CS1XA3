#!/bin/bash


if [ "$1" == feature1 ] ; then 
        find . -type f -exec ls -lSd {} +
fi

if [ "$2" == feature2 ] ; then
	git log -all --grep="homepage" && git checkout  
fi

if [ "$3" == feature3 ] ; then 
      	
fi
